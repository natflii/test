#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>
#define Task1
#ifdef Task1
#define SIZE 14
int main() {
    int arr[SIZE];
    int count = 0;
    int sum = 0;
    for (int i = 0; i < SIZE; i++) {
        scanf("%d", &arr[i]);
    }
    for (int i = 0; i < SIZE; i++) {
        if (arr[i] > 0 && arr[i] % 2 == 0) {
            count += 1;
            sum += arr[i];
        }
    }
    printf("kolichectvo chetn poloj: %d\n", count);
    printf("summ chetn poloj: %d\n", sum);
    return 0;
}
#endif Task1
